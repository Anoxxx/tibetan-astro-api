# No external dependencies are required for the Vercel serverless function
# as it uses Python's standard library (http.server, json, etc.).
# Keeping this file ensures Vercel uses the Python runtime. 